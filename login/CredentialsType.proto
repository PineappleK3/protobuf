syntax = "proto3";

/*
 * Knuddels protobuf specification v.1.0.0
 *
 * @author Bizzi
*/

enum CredentialsType {
	CredentialsType_NotSet = 0;
	CredentialsType_Password = 1;
	CredentialsType_Facebook = 2;
}